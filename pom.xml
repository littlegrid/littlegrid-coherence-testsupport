<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>org.testupport</groupId>
    <artifactId>testsupport-coherence</artifactId>
    <packaging>jar</packaging>
    <version>2.0-SNAPSHOT</version>

    <name>${project.artifactId}</name>

    <scm>
        <connection>scm:git:https://practicalblend@bitbucket.org/practicalblend/coherence-testsupport.git</connection>
        <developerConnection>scm:git:https://practicalblend@bitbucket.org/practicalblend/coherence-testsupport.git</developerConnection>
        <url>git:https://practicalblend@bitbucket.org/practicalblend/coherence-testsupport.git</url>
    </scm>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>

        <!--
                <dependency>
                    <groupId>oracle</groupId>
                    <artifactId>coherence</artifactId>
                    <version>3.5.3b465</version>
                    <scope>provided</scope>
                </dependency>
        -->
        <!--
                <dependency>
                    <groupId>oracle</groupId>
                    <artifactId>coherence</artifactId>
                    <version>3.6.1.0</version>
                    <scope>provided</scope>
                </dependency>
        -->
        <!--
                <dependency>
                    <groupId>com.oracle</groupId>
                    <artifactId>coherence</artifactId>
                    <version>3.7.0.0</version>
                    <scope>provided</scope>
                </dependency>
        -->
        <dependency>
            <groupId>com.oracle</groupId>
            <artifactId>coherence</artifactId>
            <version>${coherence.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>${install.plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${clean.plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${compiler.plugin.version}</version>
                    <executions>
                        <execution>
                            <phase>compile</phase>
                            <goals>
                                <goal>compile</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <source>${java.target.version}</source>
                        <target>${java.target.version}</target>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire.plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>${release.plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${javadoc.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <distributionManagement>
        <repository>
            <id>rainbow-release</id>
            <name>rainbow-release</name>
            <url>http://192.168.1.151:8081/nexus/content/repositories/releases</url>
        </repository>
        <snapshotRepository>
            <id>rainbow-snapshot</id>
            <name>rainbow-snapshot</name>
            <url>http://192.168.1.151:8081/nexus/content/repositories/snapshots</url>
        </snapshotRepository>
    </distributionManagement>

    <profiles>
        <profile>
            <id>default</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <!--
                                Due to the classes being loaded into different class loaders and coupled with
                                the fact that these tests are all about testing that behaviour, the forking and
                                below argLine settings are used to avoid the perm-space from becoming full.
                            -->
                            <forkMode>always</forkMode>
                            <argLine>-Xmx768m -Xms768m -XX:MaxPermSize=512m</argLine>
                            <systemPropertyVariables>
                                <tangosol.coherence.wka.port>45123</tangosol.coherence.wka.port>
                            </systemPropertyVariables>
                        </configuration>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-checkstyle-plugin</artifactId>
                        <version>${checkstyle.version}</version>
                        <configuration>
                            <configLocation>${checkstyle.configuration.file}</configLocation>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>customArgLineToBeUsed</id>
            <activation>
                <property>
                    <name>argLine</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <forkMode>always</forkMode>
                            <!--
                                If using this custom argLine profile (typically to support command-line override),
                                then in additional to your own user specified settings you should also include
                                those from the default profile as sensible memory defaults.
                            -->
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <properties>
        <java.target.version>1.6</java.target.version>

        <!-- JAR versions -->
        <coherence.version>3.7.1.0</coherence.version>
        <junit.version>4.8.2</junit.version>

        <!-- Plugin versions -->
        <clean.plugin.version>2.4.1</clean.plugin.version>
        <install.plugin.version>2.3.1</install.plugin.version>
        <release.plugin.version>2.1</release.plugin.version>
        <compiler.plugin.version>2.3.2</compiler.plugin.version>
        <surefire.plugin.version>2.10</surefire.plugin.version>
        <javadoc.plugin.version>2.7</javadoc.plugin.version>
        <checkstyle.version>2.7</checkstyle.version>

        <checkstyle.configuration.file>tweaked_sun_checks.xml</checkstyle.configuration.file>
    </properties>
</project>
